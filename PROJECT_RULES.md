# プロジェクト開発ルール

## デプロイ・本番反映
- **勝手に本番環境（firebase deploy 等）への反映を行わないこと。**
- 修正や機能追加は、まず開発環境（localhost 等）で動作確認を行う。
- 本番環境への反映は、必ずユーザーの明示的な許可・指示を得てから実行する。

## 開発作業
- 本番データの数値を一時的に変更した場合（テスト用のドロップ率変更など）は、作業完了後に必ず元の値に戻す。
- 破壊的な操作を行う前には、必要に応じてバックアップを検討する。
